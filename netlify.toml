[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routing (if ever added) and direct-URL loads
# both resolve to index.html instead of a Netlify 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
