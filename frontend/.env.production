# Used by `vite build` (Netlify's build step) instead of .env's localhost default.
# Live backend, verified working (health + /predict + /predict/video all tested
# against this URL before it was set here).
VITE_API_BASE=https://trackpulse-backend-7kod.onrender.com
